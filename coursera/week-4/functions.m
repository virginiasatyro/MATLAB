% -------------------------------------------------------------------------
% Introduction to Functions
%
% exemple: myRand.m

test = myRand(2, 5); % 2 < random number < 5
test

[x ss] = myRand(2, 3)
% x é a matriz de números aleatórios
% ss recebe a soma desses números

% -------------------------------------------------------------------------
% Formal Definition
% function[out_arg1, out_arg2, ...] = function_name(in_arg1, in_arg_2, ...)

% Exemple: 
% function func, function func(in1), function [out1, out2] = func(in1, in2)
% help exist -> ajuda a descobrir se um nome de função já existe

% -------------------------------------------------------------------------
% Subfunctions